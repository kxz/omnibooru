(function() {
  Danbooru.Sources = {};

  Danbooru.Sources.get = function(url) {
    $.get("<%= Danbooru::Application.routes.url_helpers.source_path :format => :json %>", {
      url: url
    }).success(function(data) {
    }).error(function(data) {
    });
  }
})();

